server:
  port: 8101

logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} ${LOG_LEVEL_PATTERN:-%5p} %m%n"

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8902/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"